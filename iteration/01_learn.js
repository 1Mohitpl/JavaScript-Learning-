// for loop 

for (let index = 0; index <= 10; index++) {
    const element = index;
    console.log(element);
    
}

// break and continew 

for (let index = 0; index <=10; index++) {
    if (index == 4) {

        console.log(`it is found`)
        continue;
        
    }
    console.log(`value of index is : ${index}`)
    
}

